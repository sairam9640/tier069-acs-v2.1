# ==============================================================================
# TR-069 ACS & ISP SAAS PLATFORM - PRODUCTION ENVIRONMENT CONFIGURATION
# ==============================================================================

# Node Environment ('production' | 'development' | 'test')
NODE_ENV=production

# Server Ports
PORT=3000
CWMP_PORT=7547
HTTPS_PORT=7548

# MongoDB Database Connection String
MONGO_URI=mongodb://127.0.0.1:27017/tr069_acs

# ------------------------------------------------------------------------------
# 🔐 CRYPTOGRAPHIC AUTHENTICATION SECRETS (MANDATORY IN PRODUCTION)
# ------------------------------------------------------------------------------
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=c08a9f24d35e4789b91e92d8471b059372ef019234857bdf8492048591823758

# Optional custom secret overrides
SUPER_ADMIN_SECRET=
OPERATOR_SECRET=
REFRESH_SECRET=

# ------------------------------------------------------------------------------
# 👤 INITIAL USER SEEDING (Used on first startup if DB is uninitialized)
# ------------------------------------------------------------------------------
# Super Admin Production Email Authentication
SUPER_ADMIN_EMAIL=kanugulasairam2004@gmail.com
SUPER_ADMIN_PHONE=9949666907

# ------------------------------------------------------------------------------
# 📧 GMAIL SMTP & NODEMAILER CONFIGURATION (Production Email OTP Delivery)
# ------------------------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=bsnlott1@gmail.com
SMTP_PASS=your_16_char_google_app_password
SMTP_FROM="VRV ACS" <bsnlott1@gmail.com>
# ------------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=
TELEGRAM_DEFAULT_CHAT_ID=
WHATSAPP_ENABLED=true
